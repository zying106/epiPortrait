% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/06_visualization.R
\name{plot_shift_comparison}
\alias{plot_shift_comparison}
\title{Plot Group-Level Coverage Overlay for Shape Shift Validation}
\usage{
plot_shift_comparison(
  se,
  peak_id,
  group_var = "Condition",
  group1,
  group2,
  extend_bp = 1000,
  se_ribbon = TRUE
)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} object from \code{build_portrait_matrix()}.}

\item{peak_id}{Character. The peak ID to plot (must match \code{rownames(se)}).}

\item{group_var}{Character. Column in \code{colData(se)} with the grouping variable.}

\item{group1}{Character. Identifier for the first group (e.g., reference).}

\item{group2}{Character. Identifier for the second group (e.g., treatment).}

\item{extend_bp}{Numeric. Base pairs to extend the view beyond the peak boundaries.}

\item{se_ribbon}{Logical. If \code{TRUE}, plot mean \eqn{\pm} SEM ribbons.}
}
\value{
A \code{ggplot} object.
}
\description{
Overlays mean coverage profiles of two experimental groups on
the same panel with translucent ribbons (mean \eqn{\pm} SEM). This is the
most intuitive way to visually confirm a Shape Shift event: instead of
faceting by sample, the two group averages are directly superimposed so
differences in peak geometry are immediately apparent.
}
