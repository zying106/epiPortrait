% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_object_contract.R
\name{get_call_results}
\alias{get_call_results}
\title{Extract Call Results as a Flat Table}
\usage{
get_call_results(se, feature = "Intensity", long = FALSE)
}
\arguments{
\item{se}{A SummarizedExperiment after \code{call_super_domains}.}

\item{feature}{Character. Feature name (default "Intensity").}

\item{long}{Logical. If TRUE, returns long format (one row per domain x
group); if FALSE, wide format with one column per group.}
}
\value{
A \code{data.frame}.
}
\description{
Returns per-domain call results for a feature in long or wide
form: for each condition group, the call, replicate support and rank.
}
\examples{
data(example_se)
se <- call_super_domains(example_se, feature = "Intensity",
                         mode = "per_group", group_var = "Condition",
                         verbose = FALSE)
head(get_call_results(se, "Intensity"))
head(get_call_results(se, "Intensity", long = TRUE))
}
