% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13_enrichment_gsea.R
\name{gsea_epi_genes}
\alias{gsea_epi_genes}
\title{Ranked Functional Enrichment of Domain-Derived Gene Scores}
\usage{
gsea_epi_genes(
  ranked,
  universe,
  org_db = NULL,
  key_type = NULL,
  database = "GO",
  ontology = "BP",
  min_gs_size = 10,
  max_gs_size = 500,
  p_adjust_method = "BH",
  padj_cutoff = 0.05,
  seed = 1,
  min_ranked = 100,
  verbose = FALSE
)
}
\arguments{
\item{ranked}{A data.frame with \code{gene_id} and \code{score} columns
(from \code{rank_epi_genes()}), or a named numeric vector.}

\item{universe}{Character vector of testable background genes (from
\code{get_domain_gene_universe()}); the ranked list is intersected with it.}

\item{org_db, key_type}{OrgDb package name and keytype; see
\code{\link{get_domain_gene_universe}}.}

\item{database}{Character. Only "GO" is implemented.}

\item{ontology}{Character. One of "BP", "MF", "CC".}

\item{min_gs_size, max_gs_size}{Integer. Gene-set size filter; \code{max_gs_size}
is capped at the ranked-list size.}

\item{p_adjust_method}{Character. Multiple-testing method (default "BH").}

\item{padj_cutoff}{Numeric. Adjusted-p threshold for the \code{significant}
flag; all tested terms are returned.}

\item{seed}{Integer. RNG seed for the permutation test, applied with
\code{withr::with_seed()} so the caller's RNG state is not modified. The
seed is recorded in the provenance.}

\item{min_ranked}{Integer. Minimum ranked genes; below it a structured empty
result is returned (never an error).}

\item{verbose}{Logical. Passed to \code{clusterProfiler::gseGO}.}
}
\value{
An \code{epi_enrichment} object with \code{$result} holding NES as
the effect size and the clusterProfiler object in \code{$object}.
}
\description{
GSEA of a signed gene-level ranking produced by
\code{\link{rank_epi_genes}}. Unlike ORA on discrete phenotype classes,
this uses each domain's actual continuous value, so the result does not
depend on where a "Super" cutoff was drawn. The ranked list is restricted
to the domain-derived testable universe, and the gene score sign is
inherited from \code{score_col} (positive = gain / expansion).
}
\examples{
if (requireNamespace("clusterProfiler", quietly = TRUE) &&
    requireNamespace("GO.db", quietly = TRUE) &&
    requireNamespace("org.Hs.eg.db", quietly = TRUE)) {
  universe <- head(AnnotationDbi::keys(org.Hs.eg.db::org.Hs.eg.db,
                                       keytype = "ENTREZID"), 2000)
  ranked <- data.frame(gene_id = universe,
                       score = seq_along(universe) - length(universe) / 2)
  res <- gsea_epi_genes(ranked, universe = universe,
                        org_db = "org.Hs.eg.db", key_type = "ENTREZID")
  head(res$result)
}
}
\seealso{
\code{\link{rank_epi_genes}}, \code{\link{enrich_epi_genes}}
}
