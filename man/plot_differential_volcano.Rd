% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09c_differential.R
\name{plot_differential_volcano}
\alias{plot_differential_volcano}
\title{Differential Domain Volcano Plot}
\usage{
plot_differential_volcano(
  se,
  feature = "Intensity",
  logFC_col = NULL,
  padj_col = NULL,
  label_n = 0
)
}
\arguments{
\item{se}{A SummarizedExperiment after \code{analyze_differential_domains()}.}

\item{feature}{Character. Differential feature column (default "Intensity").}

\item{logFC_col, padj_col}{Character. Column names for logFC and adjusted
P-value (default constructed from \code{feature}).}

\item{label_n}{Integer. Number of top domains to label with
\code{top_candidate_gene_symbol} / gene symbol if available (default 0).}
}
\value{
A ggplot object.
}
\description{
Volcano plot of a differential-domain analysis performed by
\code{\link{analyze_differential_domains}}, using the stored \code{DiffStatus}
labels with the package's publication palette. Domains are coloured by
Gain / Loss / NS (grey).
}
\examples{
data(example_se)
if (requireNamespace("limma", quietly = TRUE)) {
  se <- analyze_differential_domains(
    example_se, group_var = "Condition",
    ref_group = "Control", target_group = "Treatment")
  plot_differential_volcano(se, label_n = 5)
}
}
