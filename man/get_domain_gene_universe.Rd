% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12_enrichment_ora.R
\name{get_domain_gene_universe}
\alias{get_domain_gene_universe}
\title{Build the Testable Gene Universe of an epiPortrait Object}
\usage{
get_domain_gene_universe(
  se,
  relations = NULL,
  mark = NULL,
  domains = NULL,
  max_per_domain = NULL,
  nearest_tss_cutoff_bp = getOption("epiPortrait.nearest_tss_cutoff_bp", 10000),
  min_evidence_tier = 1L,
  restrict_testable = TRUE,
  org_db = NULL,
  key_type = NULL
)
}
\arguments{
\item{se}{A SummarizedExperiment after \code{annotate_epi_domains()}.}

\item{relations}{Character or NULL. Evidence relation types (see
\code{\link{get_domain_genes}}); NULL uses mark-aware defaults.}

\item{mark}{Character or NULL. Mark name for mark-aware defaults
(e.g. "H3K27ac", "H3K4me3", "H3K27me3").}

\item{domains}{Logical vector (length nrow(se)) or NULL. Restrict the
universe to a subset of domains (default: all domains).}

\item{max_per_domain}{Integer or NULL. Optional per-domain gene cap.}

\item{nearest_tss_cutoff_bp}{Numeric. Proximal-TSS cutoff for
\code{nearest_tss} links (default 10000).}

\item{min_evidence_tier}{Integer in \code{0:4}. Drop links below this tier
(default 1: a far nearest-TSS link alone does not qualify a gene).}

\item{restrict_testable}{Logical. Intersect with the annotation database key
space (default TRUE), so only testable genes enter the ORA background.}

\item{org_db}{Character or NULL. OrgDb package name; NULL is resolved from
the object's annotation provenance.}

\item{key_type}{Character or NULL. Gene ID keytype; NULL is resolved from
the annotation provenance (ENTREZID for the built-in genomes).}
}
\value{
A character vector of gene IDs with class
\code{epi_gene_universe} and attributes recording the link and restriction
statistics.
}
\description{
Returns the genes linked to the object's domains (the genes
that "had the opportunity to be selected" under the same analysis design),
optionally restricted to genes present in the annotation database. This is
the background for \code{enrich_epi_genes()} / \code{enrich_epi_domains()};
using all genes of an organism instead would ignore the domain universe and
inflate enrichment.
}
\examples{
data(example_se)
if (requireNamespace("TxDb.Hsapiens.UCSC.hg38.knownGene", quietly = TRUE) &&
    requireNamespace("org.Hs.eg.db", quietly = TRUE)) {
  se <- annotate_epi_domains(example_se, genome = "hg38")
  u <- get_domain_gene_universe(se)
  length(u)
}
}
\seealso{
\code{\link{enrich_epi_domains}}
}
