% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_annotation.R
\name{get_domain_genes}
\alias{get_domain_genes}
\title{Extract and Prioritize Candidate Genes for Domains}
\usage{
get_domain_genes(
  se,
  domains = NULL,
  relations = c("nearest_tss", "promoter_overlap", "gene_body_overlap",
    "fully_contained", "bedpe_promoter_contact"),
  group = NULL,
  expression_priority = c("none", "expressed_first", "high_expression_first"),
  min_expression = NULL,
  unique_genes = TRUE
)
}
\arguments{
\item{se}{A SummarizedExperiment after \code{annotate_epi_domains()}.}

\item{domains}{Logical vector (length nrow(se)) selecting which domains to
consider, or NULL for all.}

\item{relations}{Character. Evidence relation types to include
(default all: nearest_tss, promoter_overlap, gene_body_overlap,
fully_contained, bedpe_promoter_contact).}

\item{group}{Character or NULL. Condition group whose expression is used for
prioritization (NULL = no expression).}

\item{expression_priority}{Character. "none" (default), "expressed_first",
or "high_expression_first".}

\item{min_expression}{Numeric or NULL. Descriptive threshold for
\code{expression_status} = "Expressed". For TPM/CPM with
\code{expression_priority = "expressed_first"}, a value like 1 is a
convenience default. For VST / rlog / normalized_counts, expressed_first
requires an explicit user threshold (no universal meaning of 1). For
\code{"high_expression_first"} no threshold is needed.}

\item{unique_genes}{Logical. Collapse to unique genes (default TRUE),
retaining the highest-priority domain association per gene.}
}
\value{
A data.frame: domain_id, gene_id, gene_symbol, relation_types,
bedpe_supported, bedpe_support_count, expression_value,
expression_status, expression_rank, candidate_priority.
}
\description{
Extracts candidate (domain-associated) genes from the stored
domain-gene evidence (metadata(se)$domain_gene_links), optionally applying
RNA-seq expression prioritization. Transparent ordinal prioritization:
evidence relationships come first, expression is a secondary tie-breaker;
no black-box composite score is computed.
}
\examples{
data(example_se)
if (requireNamespace("TxDb.Hsapiens.UCSC.hg38.knownGene", quietly = TRUE)) {
  se <- annotate_epi_domains(example_se, genome = "hg38")
  head(get_domain_genes(se))
}
}
