% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12_temporal.R
\name{test_temporal_shape_shift}
\alias{test_temporal_shape_shift}
\title{Detect Temporal or Dose-Response Shape Drift}
\usage{
test_temporal_shape_shift(
  se,
  time_var = "Timepoint",
  block_var = NULL,
  poly_order = 2,
  workers = 1
)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} object from \code{build_portrait_matrix()}.}

\item{time_var}{Character. Column in \code{colData(se)} with ordered
numeric timepoints or doses (e.g., \code{c(0, 2, 6, 24)}).}

\item{block_var}{Character or NULL. Optional blocking variable (e.g., patient).}

\item{poly_order}{Integer. Maximum polynomial order to test. Default 2
(linear + quadratic). Set to 3 to also test cubic trends.}

\item{workers}{Number of threads for parallel processing (default: 1).}
}
\value{
A \code{data.frame} with columns:
\item{Linear_Score}{Pillai trace for the linear trend.}
\item{Linear_Pval}{P-value for the linear trend.}
\item{Quadratic_Score}{Pillai trace for the quadratic trend (if \code{poly_order >= 2}).}
\item{Quadratic_Pval}{P-value for the quadratic trend.}
... plus genomic coordinates, adj.P.Val, and Status.
}
\description{
Tests whether 4D geometric features drift systematically across
ordered conditions (timepoints, doses). Uses polynomial contrasts in MANOVA
to partition multivariate variance into linear and quadratic trend components,
detecting monotonic broadening/sharpening and non-linear patterns (e.g.,
peak-then-collapse) that two-group comparisons miss.
}
