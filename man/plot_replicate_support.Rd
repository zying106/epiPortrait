% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_visualization.R
\name{plot_replicate_support}
\alias{plot_replicate_support}
\title{Plot Replicate-Support Distribution}
\usage{
plot_replicate_support(
  se,
  feature = "Intensity",
  group = NULL,
  group_var = "Condition",
  min_support = 1,
  show_counts = TRUE
)
}
\arguments{
\item{se}{A SummarizedExperiment after \code{call_super_domains} per_group.}

\item{feature}{Character. Feature (default "Intensity").}

\item{group}{Character. Condition group (default first group).}

\item{group_var}{Character. colData column for groups.}

\item{min_support}{Integer. Drop support levels with fewer than this many
supporting replicates. The 0/k bin (domains never super in any replicate)
usually dominates the bar and is grey; set \code{min_support = 1} (default)
to zoom into the replicate-aware distribution.}

\item{show_counts}{Logical. Draw the domain count above each bar
(default TRUE).}
}
\value{
A ggplot.
}
\description{
Bar plot of the fraction of domains with each replicate-support
level (0/k, 1/k, ..., k/k), stacked by the final group call. Demonstrates
replicate-aware calling and the effect of the support rule.
}
\examples{
data(example_se)
se <- call_super_domains(example_se, feature = "Intensity",
                         mode = "per_group", group_var = "Condition",
                         verbose = FALSE)
plot_replicate_support(se, feature = "Intensity", group = "Control")
}
