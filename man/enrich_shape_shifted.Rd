% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_enrichment.R
\name{enrich_shape_shifted}
\alias{enrich_shape_shifted}
\title{Enrichment Analysis for Shape-Shifted Regions}
\usage{
enrich_shape_shifted(
  shift_res,
  organism = c("human", "mouse"),
  ont = "BP",
  p_cutoff = 0.05,
  q_cutoff = 0.1,
  split_by_class = TRUE
)
}
\arguments{
\item{shift_res}{A data.frame from \code{classify_shape_shift()}, or any
data.frame containing a \code{SYMBOL} column (gene symbols) and optionally
a \code{Shape_Class} column for stratified enrichment.}

\item{organism}{Character. Either \code{"human"} (default) or \code{"mouse"}.}

\item{ont}{Character. GO sub-ontology: \code{"BP"} (default), \code{"MF"},
or \code{"CC"}. For all three, run the function separately.}

\item{p_cutoff}{Numeric. Adjusted P-value cutoff (default: 0.05).}

\item{q_cutoff}{Numeric. Q-value cutoff (default: 0.1).}

\item{split_by_class}{Logical. If \code{TRUE} and \code{Shape_Class} column
exists, run enrichment per class (default: \code{TRUE}).}
}
\value{
A list with components:
\item{go}{GO enrichment results (or list of results if split by class).}
\item{kegg}{KEGG enrichment results (or list of results if split by class).}
\item{by_class}{Logical indicating whether results are split by Shape_Class.}
}
\description{
Runs GO and KEGG pathway enrichment on the target genes of
shape-shifted regions. Wraps \code{clusterProfiler} to provide a one-step
biological interpretation pipeline. By default, enrichment is run separately
for each Shape_Class category (when available), enabling class-specific
functional interpretation.
}
