% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_differential_shape.R
\name{test_differential_feature}
\alias{test_differential_feature}
\title{Perform Differential Test on a Single Geometric Feature}
\usage{
test_differential_feature(
  se,
  feature = "Intensity",
  group_var = "Condition",
  target_group,
  ref_group,
  method = "limma",
  block_var = NULL,
  workers = 1
)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} object from \code{build_portrait_matrix()}.}

\item{feature}{Character. The assay/feature to test (default: "Intensity").}

\item{group_var}{Character. The column in \code{colData(se)} containing the grouping variable.}

\item{target_group}{Character. The treatment group (numerator for FC).}

\item{ref_group}{Character. The reference/control group (denominator for FC).}

\item{method}{Character. The statistical test: "limma" (default), "t.test", or "wilcox".}

\item{block_var}{Character or NULL. Optional column in \code{colData(se)} specifying a
blocking/pairing variable (e.g., patient ID). For limma, added as a covariate in the
design matrix. For t.test and wilcox, enables paired testing.}

\item{workers}{Integer. Number of threads for parallel processing (default: 1).}
}
\value{
A \code{data.frame} containing logFC, P.Value, and adj.P.Val.
}
\description{
Performs a fast standard differential test on a single geometric dimension.
Supports "limma" (recommended for small samples), "t.test", or "wilcox". When a blocking
variable is provided, it is included as a covariate (limma) or used to compute paired
statistics (t.test/wilcox).
}
