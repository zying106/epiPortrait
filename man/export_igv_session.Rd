% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_export.R
\name{export_igv_session}
\alias{export_igv_session}
\title{Export Shape-Shifted Regions to IGV Session}
\usage{
export_igv_session(
  shift_res,
  se,
  genome = "hg38",
  session_file = "epiPortrait_session.xml",
  bed_file = NULL,
  fdr_cutoff = 0.05,
  locus = NULL,
  label_var = "Condition"
)
}
\arguments{
\item{shift_res}{A data.frame from \code{classify_shape_shift()} containing
classified shape-shift results with genomic coordinates.}

\item{se}{A \code{SummarizedExperiment} object from \code{build_portrait_matrix()}.}

\item{genome}{Character. Reference genome (e.g., \code{"hg38"}, \code{"hg19"},
\code{"mm10"}).}

\item{session_file}{Character. Path for the output IGV session XML.}

\item{bed_file}{Character. Path for the output annotation BED. Defaults to
the session file stem with \code{_annotation.bed} suffix.}

\item{fdr_cutoff}{Numeric. Only include peaks with adj.P.Val below this
threshold (default: 0.05).}

\item{locus}{Character or NULL. Initial view locus (e.g., \code{"chr1:1000-2000"}).
If \code{NULL} (default), zooms to the top shifted peak.}

\item{label_var}{Character. Column in \code{colData(se)} used for track group
labels (default: \code{"Condition"}).}
}
\value{
Invisibly, the path to the session XML file.
}
\description{
Generates an IGV session XML with BigWig tracks for all samples
and a BED annotation track of shape-shifted regions colored by Shape_Class.
Also writes the accompanying BED file. Double-click the XML to open
everything in IGV for direct visual validation.
}
