% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_annotation.R
\name{annotate_epi_peaks}
\alias{annotate_epi_peaks}
\title{Annotate epiPortrait Geometric Features with Target Genes}
\usage{
annotate_epi_peaks(
  gr,
  genome = "hg38",
  custom_txdb = NULL,
  custom_annoDb = NULL,
  tss_region = c(-3000, 3000),
  return_format = c("dataframe", "csAnno")
)
}
\arguments{
\item{gr}{A GRanges object or a data.frame with 'seqnames', 'start', 'end' columns
(such as the output from test_global_shape_shift).}

\item{genome}{A character string specifying the genome shortcut. Options are "hg38",
"hg19", "mm10", or "custom".}

\item{custom_txdb}{A TxDb object. Required only if genome = "custom".}

\item{custom_annoDb}{A character string specifying the annotation package name
(e.g., "org.At.tair.db"). Required only if genome = "custom".}

\item{tss_region}{A numeric vector of length 2 defining the TSS region (default: c(-3000, 3000)).}

\item{return_format}{Character. Either "dataframe" (default, merges annotations with your 4D features)
or "csAnno" (returns the raw ChIPseeker S4 object for plotting).}
}
\value{
A data.frame or a csAnno object depending on \code{return_format}.
}
\description{
Maps consensus peaks or shape-shifted regions (e.g., broad H3K27ac domains)
to their nearest genomic features and target genes using ChIPseeker. Supports smart
lazy-loading for common genomes and custom TxDb objects.
}
\examples{
\dontrun{
# Example: Annotating significantly shifted domains
shift_res <- test_global_shape_shift(portrait_se, group_var = "Condition")
sig_peaks <- shift_res[shift_res$adj.P.Val < 0.05, ]

# Get detailed annotation table
anno_df <- annotate_epi_peaks(sig_peaks, genome = "hg38")

# Get csAnno object for plotting (e.g., ChIPseeker::plotAnnoPie(anno_obj))
anno_obj <- annotate_epi_peaks(sig_peaks, genome = "hg38", return_format = "csAnno")
}

}
