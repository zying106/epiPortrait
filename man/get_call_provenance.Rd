% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_object_contract.R
\name{get_call_provenance}
\alias{get_call_provenance}
\title{Extract Super-Domain Call Provenance}
\usage{
get_call_provenance(se, feature = "Intensity")
}
\arguments{
\item{se}{A SummarizedExperiment after \code{call_super_domains}.}

\item{feature}{Character. Feature name (default "Intensity").}
}
\value{
A list (the stored provenance), or NULL if unavailable.
}
\description{
Returns the provenance for a feature's super-domain calls
(cutoff, stability interval, quality, per-group replicate calls), stored in
\code{metadata(se)$superdomain_calls}.
}
\examples{
data(example_se)
se <- call_super_domains(example_se, feature = "Intensity", verbose = FALSE)
get_call_provenance(se, "Intensity")$method
}
