# Run testthat + compute code coverage on push
name: Test coverage

on:
  push:
    branches: [main, master, dev]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  coverage:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      _R_CHECK_FORCE_SUGGESTS_: false

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      # epiPortrait depends on Bioconductor packages: register the
      # Bioconductor repositories before resolving dependencies.
      - uses: r-lib/actions/setup-bioc@v2

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: |
            any::covr
            any::BiocManager
            any::testthat
          needs: check

      - name: Coverage
        run: |
          covr::codecov(token = "${{ secrets.CODECOV_TOKEN }}", quiet = FALSE)
        shell: Rscript {0}
